# LLM summarization (Google Gemini — free tier)
GEMINI_API_KEY=

# Email delivery (Gmail SMTP)
EMAIL_FROM=
EMAIL_TO=
GMAIL_APP_PASSWORD=

# Optional overrides (defaults are fine for most users)
# LOG_LEVEL=INFO
# DIGEST_OUTPUT_DIR=output
# DIGEST_MAX_ITEMS=20
# TOPICS_CONFIG_PATH=config/topics.yaml
# SUMMARY_MODE=extractive
